<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>update User</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-5">
      <div class="nav mb-10">
        <a href="/read" class="text-yellow-500">Read Users</a>
      </div>
      <h1 class="text-3xl tracking-tighter mb-3">Update User</h1>
      <form action="/update/<%= user._id %>" method="post" class="flex flex-col gap-4 max-w-lg">
        <input
          class="px-5 py-3 bg-zinc-800 rounded-lg outline-none"
          type="text"
          name="name"
          placeholder="Name"
          value="<%= user.name %>"
        />
        <input
          class="px-5 py-3 bg-zinc-800 rounded-lg outline-none"
          type="text"
          name="email"
          placeholder="Email"
          value="<%= user.email %>"
        />
        <input
          class="px-5 py-3 bg-zinc-800 rounded-lg outline-none"
          type="text"
          name="image"
          placeholder="Image URL"
          value="<%= user.image %>"
        />
        <button
          type="submit"
          class="bg-yellow-500 px-5 py-2 rounded-lg text-black"
        >
          Update User
        </button>
      </form>
    </div>
  </body>
</html>
